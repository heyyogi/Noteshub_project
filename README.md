
# 📚 NotesHub

NotesHub is a web application where users can upload and download books and handwritten notes. It includes authentication features (login/signup) and provides a simple interface for managing study materials.

# 🚀 Features


- 📖 Upload and download books & handwritten notes
- 🔐 User authentication (login/signup)
- 🖥️ Frontend built with React + Node.js
- 🗄️ Backend powered by PostgreSQL
- 🐳 Deployed as a two‑tier Docker applicatio
# 🛠️ Tech Stack
- Frontend: React, TailwindCSS, Node.js
- Backend: Node.js (Express), PostgreSQL
- Deployment: Docker (multi‑container setup)

## Run Locally

Clone the project

```bash
  git clone https://link-to-project
```

Go to the project directory

```bash
  cd my-project
```

run backend docker-compose file

```bash
  docker-compose up
```

run frontend docker file


```bash
- Build the image:
docker build -t my-react-app .
```

```bash
- run the image:
docker run -d -p 3000:80 my-react-app
```
